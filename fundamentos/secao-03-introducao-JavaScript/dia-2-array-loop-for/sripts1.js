let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia'];

console.log ('Precisa de : ');

for (let index = 0; index < groceryList.length; index += 1) {
    let message = groceryList[index];
    console.log (message);
}
